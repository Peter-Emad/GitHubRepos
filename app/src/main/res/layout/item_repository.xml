<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="20dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/imgRepoContainer"
        android:layout_width="42dp"
        android:layout_height="42dp"
        app:cardCornerRadius="6dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/imgRepo"
            android:layout_width="42dp"
            android:layout_height="42dp" />
    </androidx.cardview.widget.CardView>


    <TextView
        android:id="@+id/txtRepoTitle"
        style="@style/Home.Search.Content.State.Repo.Title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/imgRepoContainer"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/txtRepoDescription"
        style="@style/Home.Search.Content.State.Repo.Description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@id/imgRepoContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/txtRepoTitle"
        app:layout_constraintTop_toBottomOf="@id/txtRepoTitle" />

</androidx.constraintlayout.widget.ConstraintLayout>